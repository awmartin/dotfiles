# sources all the scripts in dotfiles/config

config_dir="$HOME/dotfiles/config"
config_files=`ls -d -- $config_dir/*`

for file in $config_files; do
  if [ -e "$file" ]; then
    source ${file}
  fi
done
